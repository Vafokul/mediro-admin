// Placeholder test file. The full admin app boots Supabase + GetStorage
// which is heavy for unit tests — keep this as a smoke test slot for now.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
